spring:
  liquibase:
    change-log: classpath:/migration/master-changelog.xml
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:cloud_config}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:linh2002}
  profiles:
    active: jdbc
  cloud:
    config:
      server:
        jdbc:
          sql: SELECT prop_key, value_config FROM data_cloud WHERE application=? AND profile_client=? AND label=?
          order: 0
  jpa:
    show-sql: true